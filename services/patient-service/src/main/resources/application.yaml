spring:
  application:
    name: patient-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3309/patient_service_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  docker:
    compose:
      enabled: true
      file: services/patient-service/docker-compose.yaml
      lifecycle-management: start-and-stop
      wait:
        timeout: 2m
      stop:
        command: down
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    default-property-inclusion: non_null
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

server:
  port: ${SERVER_PORT:8085}
  servlet:
    context-path: /

app:
  jwt:
    secret: ${APP_JWT_SECRET:smart-healthcare-shared-jwt-secret-change-this-in-production-must-be-at-least-32-bytes}
  security:
    allowed-origins: ${APP_SECURITY_ALLOWED_ORIGINS:http://localhost:*,http://127.0.0.1:*}
  upload:
    dir: ${APP_UPLOAD_DIR:/app/uploads}

logging:
  level:
    root: INFO
    com.smarthealthcare.patient_service: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
