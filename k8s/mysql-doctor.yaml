apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-doctor-pv
spec:
  storageClassName: ""
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/smart-healthcare/mysql-doctor
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-doctor-pvc
  namespace: smart-healthcare
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: mysql-doctor-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-doctor
  namespace: smart-healthcare
  labels:
    app: mysql-doctor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-doctor
  template:
    metadata:
      labels:
        app: mysql-doctor
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: doctor_service_db
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_DOCTOR_USERNAME
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_DOCTOR_PASSWORD
          volumeMounts:
            - name: mysql-doctor-data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 60
            periodSeconds: 20
      volumes:
        - name: mysql-doctor-data
          persistentVolumeClaim:
            claimName: mysql-doctor-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-doctor
  namespace: smart-healthcare
spec:
  type: ClusterIP
  selector:
    app: mysql-doctor
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
