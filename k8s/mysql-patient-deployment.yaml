apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-patient
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-patient
  template:
    metadata:
      labels:
        app: mysql-patient
    spec:
      containers:
        - name: mysql-patient
          image: mysql:8.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "123456"
            - name: MYSQL_DATABASE
              value: patient_service_db
          volumeMounts:
            - name: mysql-patient-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-patient-storage
          persistentVolumeClaim:
            claimName: mysql-patient-pvc
