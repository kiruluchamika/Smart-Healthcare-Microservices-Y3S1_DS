apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-patient-pv
spec:
  storageClassName: ""
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/smart-healthcare/mysql-patient
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-patient-pvc
  namespace: smart-healthcare
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: mysql-patient-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-patient
  namespace: smart-healthcare
  labels:
    app: mysql-patient
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-patient
  template:
    metadata:
      labels:
        app: mysql-patient
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: patient_service_db
          volumeMounts:
            - name: mysql-patient-data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 60
            periodSeconds: 20
      volumes:
        - name: mysql-patient-data
          persistentVolumeClaim:
            claimName: mysql-patient-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-patient
  namespace: smart-healthcare
spec:
  type: ClusterIP
  selector:
    app: mysql-patient
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
