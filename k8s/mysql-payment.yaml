apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-payment-pv
spec:
  storageClassName: ""
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/smart-healthcare/mysql-payment
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-payment-pvc
  namespace: smart-healthcare
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: mysql-payment-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-payment
  namespace: smart-healthcare
  labels:
    app: mysql-payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-payment
  template:
    metadata:
      labels:
        app: mysql-payment
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: payment_service_db
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_PAYMENT_USERNAME
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_PAYMENT_PASSWORD
          volumeMounts:
            - name: mysql-payment-data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 60
            periodSeconds: 20
      volumes:
        - name: mysql-payment-data
          persistentVolumeClaim:
            claimName: mysql-payment-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-payment
  namespace: smart-healthcare
spec:
  type: ClusterIP
  selector:
    app: mysql-payment
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
