apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-notification-pv
spec:
  storageClassName: ""
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/smart-healthcare/mysql-notification
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-notification-pvc
  namespace: smart-healthcare
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: mysql-notification-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-notification
  namespace: smart-healthcare
  labels:
    app: mysql-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-notification
  template:
    metadata:
      labels:
        app: mysql-notification
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: notification_service_db
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_NOTIFICATION_USERNAME
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: MYSQL_NOTIFICATION_PASSWORD
          volumeMounts:
            - name: mysql-notification-data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
            initialDelaySeconds: 60
            periodSeconds: 20
      volumes:
        - name: mysql-notification-data
          persistentVolumeClaim:
            claimName: mysql-notification-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-notification
  namespace: smart-healthcare
spec:
  type: ClusterIP
  selector:
    app: mysql-notification
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
